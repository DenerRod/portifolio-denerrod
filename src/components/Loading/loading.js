import React from "react";
import "./loading.css"

class Loading extends React.Component {
    render() {
        return(
            <div className="loading centralize ">Loading...</div>
        )
    }
}

export default Loading;